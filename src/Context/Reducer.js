import { SEARCH_IMAGE } from "./Actions"


const reducer = (state, action) => {
    if(action.type === SEARCH_IMAGE) {
        //something will come here later 
    } else {
        //Throws an error later
    }
}

export default reducer